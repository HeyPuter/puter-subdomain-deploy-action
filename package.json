{
  "name": "puter-subdomain-deploy-action",
  "version": "1.0.0",
  "description": "GitHub Action for deploying a repository path to Puter FS and managing a Puter subdomain binding.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/deploy.mjs"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@heyputer/puter.js": "latest"
  }
}
